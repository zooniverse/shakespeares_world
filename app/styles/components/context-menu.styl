.context-menu
    @extend .dropdown-menu
    display: none
    top: 0
    //Using z-index creates conflict
    //with putting bg-image on body element,
    //i.e. one can see an the context menu on top-left
    //z-index: -1

    &.-active
        display: block
        //z-index: 1

    > li > a
        @extend .dropdown-menu > li > a
